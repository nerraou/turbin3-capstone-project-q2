# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=travelramp_db
# DATABASE_SCHEMA=public
# DATABASE_SSL=false                 # OPTIONAL: Enable SSL (default: false)
# DATABASE_POOL_MIN=2                # OPTIONAL: Min pool connections (default: 2)
# DATABASE_POOL_MAX=10               # OPTIONAL: Max pool connections (default: 10)

# =============================================================================
# JWT
# =============================================================================
JWT_SECRET="generate using openssl rand -hex 32"


# =============================================================================
# Wallet Encryption
# =============================================================================
WALLET_KEK="generate using openssl rand -hex 32"




NEXT_PUBLIC_RPC_URL=https://api.devnet.solana.com
TRAVELRAMP_PROGRAM_ID=YOUR_PROGRAM_ID
BACKEND_WALLET_SECRET_KEY=[1,2,3,...]